<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Book Catalog" %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %> 

    <!-- stylesheet -->
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

    <!-- Intel One Mono via Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Intel+One+Mono:wght@500;700&display=swap" rel="stylesheet">

    <!-- favicon -->
    <link rel="icon" type="image/png" href="<%= asset_path 'favicon.png' %>">

    <%= javascript_importmap_tags %>
    <%= yield :head %>

    <style>
      /* Navbar brand title */
      .brand-title {
        font-family: "Intel One Mono", monospace;
        font-weight: 700;
        font-size: 1.8rem;
        color: #000;
      }

      /* home page welcome title */
      .welcome-title {
        font-family: "Intel One Mono", monospace;
        font-weight: 700;
        font-size: 3rem;
        color: #000;
      }

      /* subtext under welcome title */
      .welcome-subtext {
        font-family: "Intel One Mono", monospace;
        font-weight: 500;
        font-size: 1.2rem;
        color: #333;
      }
    </style>
  </head>

  <body class="container mt-5">
    <% flash.each do |key, value| %>
      <div class="alert alert-<%= key == 'notice' ? 'success' : 'danger' %> alert-dismissible fade show" role="alert">
        <%= value %>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
      </div>
    <% end %>

    <nav class="navbar navbar-expand-lg navbar-light bg-light mb-4">
      <div class="container-fluid">
        <!-- brand with custom font and style -->
        <a class="navbar-brand brand-title" href="/">📚 Book Catalog</a>
        <div class="d-flex">
          <% if user_signed_in? %>
            <span class="navbar-text me-3">Logged in as <%= current_user.email %></span>
            <%= button_to "Logout", destroy_user_session_path, method: :delete, class: "btn btn-outline-danger btn-sm" %>
          <% else %>
            <%= link_to "Login", new_user_session_path, class: "btn btn-outline-primary btn-sm" %>
          <% end %>
        </div>
      </div>
    </nav>

    <%= yield %>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
